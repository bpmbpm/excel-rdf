# Зависимости и артефакты, используемые ТОЛЬКО для локального тестирования (node).
# Сам продукт — браузерный, эти файлы в репозиторий не входят.
node_modules/
package.json
package-lock.json

# Артефакты Playwright MCP и временные файлы
.playwright-mcp/
*.tmp
